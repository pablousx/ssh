name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - name: Verify version
        run: |
          test "v$(cat VERSION)" = "${GITHUB_REF_NAME}"
      - name: Build release archives
        run: |
          mkdir -p dist/sshwitch
          cp -R linux windows providers README.md ROADMAP.md LICENSE SECURITY.md CHANGELOG.md VERSION dist/sshwitch/
          tar -C dist -czf "dist/sshwitch-${GITHUB_REF_NAME}.tar.gz" sshwitch
          (cd dist && zip -qr "sshwitch-${GITHUB_REF_NAME}.zip" sshwitch)
          (cd dist && sha256sum "sshwitch-${GITHUB_REF_NAME}.tar.gz" > "sshwitch-${GITHUB_REF_NAME}.tar.gz.sha256")
          (cd dist && sha256sum "sshwitch-${GITHUB_REF_NAME}.zip" > "sshwitch-${GITHUB_REF_NAME}.zip.sha256")
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --verify-tag \
            --generate-notes \
            "dist/sshwitch-${GITHUB_REF_NAME}.tar.gz" \
            "dist/sshwitch-${GITHUB_REF_NAME}.tar.gz.sha256" \
            "dist/sshwitch-${GITHUB_REF_NAME}.zip" \
            "dist/sshwitch-${GITHUB_REF_NAME}.zip.sha256"
